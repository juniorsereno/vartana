---
en:
  imports:
    uploads:
      show:
        title: Import data
        description: Import data from your bank or budgeting app
        tabs:
          upload_csv: Upload CSV
          copy_paste: Copy & Paste
        browse_html: "<span class='font-medium text-primary'>Browse</span> to add your CSV file here"
        paste_placeholder: Paste your CSV file contents here
        download_sample: Download a sample CSV
        see_format: to see the required CSV format
        account_optional: Account (optional)
        multi_account: Multi-account import
        buttons:
          upload: Upload CSV
    confirms:
      mappings:
        create_account: Create account
        next: Next
    configurations:
      mint_import:
        pre_configured_message: We have pre-configured your Mint import for you. Please proceed to the next step.
        leave_empty: Leave empty
        labels:
          date: Date
          amount: Amount
          currency: Currency
          format: Format
          account_optional: Account (optional)
          name_optional: Name (optional)
          category_optional: Category (optional)
          tags_optional: Tags (optional)
        prompts:
          select_format: Select format
        signage:
          negative: Incomes are negative
          positive: Incomes are positive
        buttons:
          apply: Apply configuration
